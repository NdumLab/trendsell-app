# Runtime dependencies only. Test-only packages live in requirements-dev.txt so a
# production install never carries them (action plan T09).
fastapi==0.135.1
uvicorn==0.41.0
pydantic==2.12.5
SQLAlchemy==2.0.48
alembic==1.18.4
psycopg[binary]==3.3.3
python-dotenv==1.2.2
